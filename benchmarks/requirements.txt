# The bench is a subtree of diff-graph — dependencies are managed in
# the repo-root requirements.txt (which folds in the bench-only deps:
# atlassian-python-api, anthropic, pydantic, pytest-asyncio, anyio).
#
# Install once from the repo root:
#     pip install -r requirements.txt
#
# This file is kept as a pointer so anyone landing in benchmarks/
# doesn't try to install a stale, partial dependency set.

Analyze this source file and return ONLY valid JSON, no markdown, no explanation.

{{
  "name": "ClassName or module name",
  "summary": "1 sentence what this file does",
  "symbols": [
    {{
      "name": "symbolName",
      "kind": "METHOD|CLASS|FIELD|INTERFACE|ENUM|FUNCTION|MODULE",
      "signature": "full signature with types",
      "summary": "1 sentence",
      "annotations": ["@Transactional"],
      "start_line": 42,
      "end_line": 67
    }}
  ],
  "dependencies": [
    {{
      "name": "ClassName",
      "fqn": "com.example.package.ClassName",
      "usage": "1 sentence how this dependency is used in this file"
    }}
  ]
}}

Rules for "dependencies":
- Only types used as: method parameters, return types, field types, constructor injections, or generic type arguments
- EXCLUDE annotation types — if a name only appears with "@" prefix (e.g. @Entity, @Transactional, @GetMapping), do NOT include it
- EXCLUDE primitives, builtins, and standard library (java.lang.*, java.util.*, java.io.*, etc.)
- EXCLUDE framework infrastructure annotations (Spring MVC mappings, JPA annotations, Lombok, etc.)
- For Python: names used as function parameters, return types, or field types only
- For Go: struct/interface names used as parameter or return types

Rules for "fqn" (fully qualified name):
- For Java/Kotlin: copy the full import statement path (e.g. "com.flowmart.orders.model.Order")
- For Python: use "module.ClassName" from the import (e.g. "orders.model.Order")
- For Go: use "package.TypeName"
- If no import found for this name, use just the name as fqn

Rules for "usage" (per dependency):
- One sentence describing how this dependency is used in this file
- Examples: "injected via constructor, used to persist orders to DB",
            "return type of calculateTotal() and applyDiscount()",
            "thrown when the requested resource does not exist"

For files with no class structure (Python modules, Go files with only functions):
- Use "MODULE" as kind for top-level symbols
- Use filename without extension as "name"

File: {path}
Language: {lang}
Content (each line prefixed with its 1-based line number):
{content}

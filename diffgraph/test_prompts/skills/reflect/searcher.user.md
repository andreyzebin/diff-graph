---
# Baseline searcher — NO skill mounted. Searcher has only the
# bare probe/answer tools and must converge in working memory.
# Used by SKILL-002-without scenario as the reference behavior.
data: {}
---
Find the hidden integer target in the inclusive range [1, 100].
You may call `probe(guess)` up to 10 times. Submit the integer
you've identified via `answer(text="N")` — single call, run
terminates with your answer recorded.

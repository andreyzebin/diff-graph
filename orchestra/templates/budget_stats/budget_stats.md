Your own session: {tokens_in} of {max_context} LLM context window used ({pct}%). Children spawn into fresh windows; only their done() summary returns to your session.
Shared with children: {paid} of {max_tokens} tokens, {steps_used} of {max_steps} steps used. Each agent_spawn carves a slice from your remaining budget.
{wall_clock}
Typical investigator spawn: returns {returned_to_you} to your own session; carves {carved_from_shared} from the shared pool ({based_on}).{subagents}

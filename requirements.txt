openai>=1.30.0
httpx>=0.27.0
urllib3>=2.0.0
typer>=0.12.0
rich>=13.7.0
pyyaml>=6.0
pytest>=8.0.0
tree-sitter>=0.23.0
# Language grammars — one per supported source language. The per-
# language packages (`tree_sitter_<lang>`) replace the legacy
# `tree-sitter-languages` aggregator (incompatible with
# tree-sitter>=0.22's Parser API change). Outline / search / diff
# view detect language by file extension and dispatch to the
# right parser via `diffgraph.outline._get_parser`. Missing
# package at runtime → that language's outline degrades to
# "tree-sitter unavailable" but diff_read_file / diff_search
# still work (they're not grammar-dependent).
tree-sitter-python>=0.23.0
tree-sitter-java>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-c>=0.23.0
tree-sitter-cpp>=0.23.0
tree-sitter-c-sharp>=0.23.0
tree-sitter-php>=0.23.0
tree-sitter-ruby>=0.23.0
tree-sitter-kotlin>=0.23.0
tree-sitter-scala>=0.23.0
tree-sitter-bash>=0.23.0
tree-sitter-html>=0.23.0
tree-sitter-css>=0.23.0
tree-sitter-json>=0.23.0
tree-sitter-yaml>=0.23.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
truststore>=0.9.0
jsonschema>=4.20.0
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0

# ── benchmarks/ subtree (was code-review-benchmarks) ──────────────
# Bench-only deps not already pulled in by the engine above. The
# overlapping ones (openai, httpx, fastapi, uvicorn, pyyaml, typer,
# rich, truststore, jsonschema, jinja2, pytest) are satisfied by the
# entries above — kept here only where the bench needs something the
# engine doesn't.
atlassian-python-api>=3.41.4   # real Bitbucket Server PR provider
anthropic>=0.28.0             # judge LLM backend option
pydantic>=2.7.0               # scenario / config models
pytest-asyncio>=0.23.0        # async bench runner tests
anyio>=4.4.0                  # async primitives in the runner
